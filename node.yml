---
#- name: system
#  apt: name={{ item }}
#  with_items:
#    - git
- name: clone
  git: repo=https://github.com/joyent/node.git dest=/tmp/nodejs version=v0.6.8
#- name: branch
#  shell: "cd /tmp/nodejs git checkout v0.6.8"
- name: configure
  shell: "cd /tmp/nodejs ./configure --openssl-libpath=/usr/lib/ssl"
- name: install
  shell: "cd /tmp/nodejs sudo make install"
